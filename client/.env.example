# Base URL of the FlowGuard NestJS server.
# Vite inlines every VITE_-prefixed variable into the browser bundle, so only
# public values belong in this file — never an API key or a database URI.
VITE_API_URL=http://localhost:3000
